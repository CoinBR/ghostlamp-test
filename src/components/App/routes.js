// import { Route } from 'react-router-dom';

export const PATHS = {
  landing: '/',
  register: '/register',
  login: '/login',
  account: '/account',
  passwordForgot: '/forgot-password',

  myTVShows: '/my-tvshows',
};

/*
export const Routes = (props) => {
  const routes = PATHS.keys().map((key) => {
    return <Route exact path={PATHS.myTVShows} component={TVShowList} />
  })
}
*/